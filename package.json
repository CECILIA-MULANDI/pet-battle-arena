{
  "name": "pet-battle-arena",
  "version": "1.0.0",
  "description": "Blockchain Pet Battle Game - Train, Fight, Win!",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network baseTestnet",
    "deploy-local": "hardhat run scripts/deploy.js --network localhost",
    "battle": "node scripts/simulateBattle.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "hardhat": "^2.26.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.0"
  }
}
